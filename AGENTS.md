# Working rules

Codex is the primary developer. Qwen/Groq AI Helper is a delegated implementer and second-opinion reviewer.
Use tools/ai_helper.py for significant architecture, complex modules, difficult debugging, security, important tests and milestone reviews. Do not call it for tiny edits. Never blindly copy suggestions or redesign working components without evidence. Validate all advice against source, tests and requirements. Record QWEN RECOMMENDATIONS ACCEPTED, QWEN RECOMMENDATIONS REJECTED and WHY.

The user explicitly requested broad Qwen participation and actual code delegation on 2026-09-12. Use Qwen throughout substantial development stages with a generous completion budget. Give bounded implementation tasks through the implement mode; inspect drafts and test them before integration. Record actual reviews and independent decisions under docs/reviews or docs/delegation. Codex remains responsible for integration and verification.

The current session's automatic approval review rejected transmission of a selected project source file to Groq; explicit source-transmission confirmation remains pending. Successful delegation used new generic task specifications only, with no project file contents, configuration, logs or customer data. Continue within that boundary unless the user explicitly approves source transmission. Authentication credentials belong only in the API client's environment, never in prompts or files. Do not treat sanitizer output as a confidentiality guarantee or automatically execute model output.

Work milestone by milestone. Run relevant tests, launch what is available, fix issues and report concise progress. Keep domain code independent of API and connectors. Decimal money, UTC timestamps, database constraints and atomic transactions are mandatory. No secrets, customer data, private logs or environment files in prompts or version control. Groq failure cannot break SyncGuard. Use only synthetic context in reviews. Do not add infrastructure beyond this MVP.
